const { build } = require('electron-builder')

build({
  // Aqui você pode especificar as opções de build do seu aplicativo
  // Consulte a documentação do Electron Builder para obter mais informações
  // https://www.electron.build/configuration/configuration
})
